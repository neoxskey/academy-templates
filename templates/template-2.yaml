# Placeholder for template-2.yaml
# Add your .study YAML here
id: study-s3-public
info:
  name: "S3 Bucket Misconfiguration"
  author: "HackerAcademy_Seed"
  severity: medium

environment:
  type: cloud-simulation
  service: "LocalStack"

scenario:
  mission: "Find the 'backups' folder in the simulated cloud storage and download 'db.sql'."
  hint: "Use the 'aws s3 ls' command against the custom endpoint."

validation:
  type: command-output
  contains: "DATABASE_SECRET_KEY=4592"
